import React from 'react'

const Login = (props) => {
    return (
        <div className='loginContainer'>
            Login
            console.log({props.render});
        </div>
    )
}

export default Login